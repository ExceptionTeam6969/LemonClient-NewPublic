package dev.lemonclient.managers.impl;
/*
import dev.lemonclient.addon.gui.screen.TestGui;
import dev.lemonclient.addon.render.gui.InstaScreen;
import dev.lemonclient.LemonClient;

import static dev.lemonclient.LemonClient.mc;

public class GuiManager {
    public InstaScreen currentGui;
    private final TestGui harvestGui = new TestGui();

    public GuiManager() {
        LemonClient.EVENT_BUS.subscribe(this);
    }

    @EventHandler
    private void onKey(KeyEvent event) {
        if (event.key == Modules.get().get(GuiSettings.class).keyBind.get().getValue() && event.action == KeyAction.Press) {
            if (Utils.canOpenGui()) {
                this.setScreen(harvestGui);
            }
        }
    }

    public void setScreen(InstaScreen screen) {
        this.currentGui = screen;
        mc.setScreen(currentGui);
    }
}
*/
