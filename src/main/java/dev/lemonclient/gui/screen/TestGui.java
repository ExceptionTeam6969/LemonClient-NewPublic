package dev.lemonclient.gui.screen;
/*
import dev.lemonclient.addon.render.gui.InstaScreen;
import dev.lemonclient.utils.render.utils.Render2DUtils;
import dev.lemonclient.utils.render.color.Color;
import net.minecraft.client.gui.DrawContext;
import net.minecraft.client.util.math.MatrixStack;
import net.minecraft.text.Text;

public class TestGui extends InstaScreen {
    public TestGui() {
        super(Text.literal("TestGui"));
    }

    //public static ArrayList<Window> windows = new ArrayList<>();

    @Override
    public void draw(DrawContext context, MatrixStack matrices, int mouseX, int mouseY, float delta) {
        Render2DUtils.drawMainMenuShader(matrices, 0, 0, width, height);
        vanillaText.draw("这里面塞了114541个东西14514个内存114514个点114514个矩形114514个三角形", 0, 0, Color.WHITE);
        vanillaText.draw("这是测试GUI。，，", 10, 25, Color.WHITE);
        vanillaText.draw("Vanilla Canvas Test", 10, 10, Color.WHITE);
        Render2DUtils.drawGradientGlow(matrices, Color.WHITE, Color.BLUE, Color.GREEN, Color.GRAY, 30, 30, 50, 50, 5, 10f);
        Render2DUtils.drawGradientRoundShader(matrices, Color.BLUE, Color.WHITE, Color.GREEN, Color.BLACK, 90, 30, 50, 50, 5);
        Render2DUtils.drawRoundShader(matrices, 150, 30, 50, 50, 5, Color.WHITE);
        renderer.drawCircle(30, 90, 20, Color.WHITE);
        renderer.drawRoundRectLine(30, 150, 50, 50, 5, 3, Color.WHITE);


        // Render2DUtils.drawGradientGlow(matrices, Color.WHITE,Color.BLUE,Color.YELLOW,Color.GRAY,10f,10f,30f,30f,5f,0.3f);
    }

    @Override
    public boolean shouldCloseOnEsc() {
        return true;
    }
}
*/
