package dev.lemonclient.gui.screen;
/*
import net.minecraft.client.gui.DrawContext;
import net.minecraft.client.gui.screen.Screen;
import net.minecraft.client.util.math.MatrixStack;
import net.minecraft.text.Text;

public class GuiRender extends Screen {
    public GuiRender(Text text) {
        super(text);
    }

    public GuiRender() {
        super(Text.of("LemonClient"));
    }

    @Override
    public final void render(DrawContext drawContext, int mouseX, int mouseY, float tickDelta) {
        //Utils.unscaledProjection();
        draw(drawContext, drawContext.getMatrices(),mouseX, mouseY, tickDelta);
        //Utils.scaledProjection();
    }

    public void draw(DrawContext context, MatrixStack matrices, int mouseX, int mouseY, float tickDelta) {
    }
}
*/
